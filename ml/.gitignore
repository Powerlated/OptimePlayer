/target

# Generated artifacts (large, regenerable): synthetic + harvested datasets and
# trained model weights (models/<00-frame|01-event|02-hier>/). See the ML section
# of ../CLAUDE.md for the commands that produce them.
/data
/models

# Compiler crash dumps.
rustc-ice-*.txt
